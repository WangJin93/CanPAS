% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_roc.R
\name{plot_roc}
\alias{plot_roc}
\title{Time-dependent ROC Curve for a Marker}
\usage{
plot_roc(
  df,
  type = "OS",
  marker,
  predict.time = 1,
  method = c("KM", "NNE"),
  span = NULL
)
}
\arguments{
\item{df}{Data.frame with columns \code{<type>_time}, \code{<type>_status}
and \code{marker}.}

\item{type}{Endpoint family (OS/DSS/DFS/PFS/MFS) or concrete token (default \code{"OS"}).}

\item{marker}{Name of the numeric marker column.}

\item{predict.time}{Prediction horizon (same unit as \code{<type>_time},
typically years). Default 1.}

\item{method}{ROC method passed to \code{survivalROC}: \code{"KM"} (default)
or \code{"NNE"}.}

\item{span}{Span for \code{method = "NNE"} (ignored for KM).}
}
\value{
A ggplot object; the estimated AUC is attached as attribute
\code{"AUC"} of the returned object.
}
\description{
Computes a time-dependent ROC curve at a given prediction horizon with
\code{survivalROC::survivalROC} and draws it with ggplot2.
}
\details{
Rows with missing time/status/marker are removed first. The
\code{<type>_time} values are interpreted as the same unit as
\code{predict.time}.
}
\examples{
\dontrun{
   d <- cohort_merged("GSE14814", c("GAPDH", "ACTB"), type = "OS")

   g <- plot_roc(d, type = "OS", marker = "GAPDH", predict.time = 3)
   g$auc
}
}
