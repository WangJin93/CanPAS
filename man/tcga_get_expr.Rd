% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpas_extra_sources.R
\name{tcga_get_expr}
\alias{tcga_get_expr}
\title{Multi-gene TCGA expression (wide format)}
\usage{
tcga_get_expr(dataset = "LUAD", genes = "TP53")
}
\arguments{
\item{dataset}{TCGA dataset id (\code{"LUAD"} or \code{"TCGA-LUAD"}).}

\item{genes}{Character vector of gene symbols.}
}
\value{
data.frame with first column \code{sample} and one numeric column
per requested gene (columns follow the order of \code{genes}).
}
\description{
Fetches each requested gene for a TCGA project from UCSC Xena
and merges them into one wide data.frame (rows = samples).
}
\examples{
\dontrun{
   ## The expression step alone, for a TCGA project (same schema as
   ## get_expr_data() for GEO cohorts).
   e <- tcga_get_expr("LUAD", c("TP53", "GAPDH"))
   colnames(e$expr_data)
   head(e$expr_data)
}
}
